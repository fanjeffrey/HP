@model HPCN.UnionOnline.Site.ViewModels.EnrollingEnrollViewModel

@{
    ViewData["Title"] = $"已报名 - {Model.Enrollment.Name}";
}

<header>
    <h2 class="text-danger">已报名 - @Model.Enrollment.Name</h2>
    <p class="text-danger">
        @Model.Name (#@Model.EmployeeNo) 已经报过名。请勿重复报名。
    </p>
</header>