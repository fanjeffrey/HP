@inject ICartService _cartService
@{
    var userIdString = User.GetUserId();
    var userId = string.IsNullOrWhiteSpace(userIdString) ? Guid.Empty : Guid.Parse(userIdString);
}
@await _cartService.Count(userId)